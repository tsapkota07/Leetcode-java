# Complexity

How to analyze time and space.

## big-o-cheatsheet.txt

Quick reference: O(1), O(log n), O(n), O(n log n), O(n²), O(2^n), O(n!).

Growth hierarchy. What operations fit each.

## time-space-analysis.txt

How to count operations. Ignore constants, focus on dominant term.

Worst/average/best case. Space: what variables and data structures you store.

## amortized-analysis.txt

When an operation is expensive sometimes but cheap on average.

Examples: dynamic array resize, hash table collisions.

